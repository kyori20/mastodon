name: build

on:
  push:
    branches: nitiasadon

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: docker/setup-buildx-action@v1

      - uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v2
        with:
          target: build-dep
          push: true
          tags: accelforce/nitiasadon:build-dep
          cache-from: accelforce/nitiasadon:build-dep
          cache-to: type=inline,mode=max

      - uses: docker/build-push-action@v2
        with:
          push: true
          tags: |
            accelforce/nitiasadon:latest
            accelforce/nitiasadon:${{ github.sha }}
          cache-from: |
            accelforce/nitiasadon:build-dep
            accelforce/nitiasadon:latest
          cache-to: type=inline,mode=max
